{
  "name": "api",
  "version": "1.0.0",
  "module": "src/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/server.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile build/server src/server.ts",
    "seed": "bun run src/db/seed.ts"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.6.9",
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/openapi": "^1.4.14",
    "ajv": "catalog:",
    "better-auth": "catalog:",
    "drizzle-orm": "1.0.0-beta.22",
    "elysia": "catalog:",
    "livekit-server-sdk": "^2.15.1",
    "openapi-types": "^12.1.3",
    "pg": "^8.18.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/pg": "^8.16.0",
    "drizzle-kit": "1.0.0-beta.22"
  }
}
